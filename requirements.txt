# Python Dependencies for TQB Worker (Faux_Clawdbot)
#
# CHANGELOG [2026-02-14 - Integration]
# Swapped custom xet vector store for NeuroGraph cognitive memory layer.
# [2026-03-29 - Hammer/TQB — Block G Assembly]
# Added anthropic SDK. Removed HuggingFace InferenceClient dependency (model swap).

# Gradio for web interface (5.0+ required for type="messages" format)
gradio>=5.0.0
pytz>=2023.3

# HuggingFace for dataset persistence (notebook + checkpoint sync)
huggingface-hub>=0.20.0

# Anthropic SDK — Claude drives the model
anthropic>=0.40.0

# NeuroGraph cognitive memory layer
sentence-transformers>=2.2.0
msgpack>=1.0.0
scipy>=1.10.0

# Document ingestion (universal_ingestor)
beautifulsoup4>=4.12.0
PyPDF2>=3.0.0

# Additional utilities
pydantic>=2.0.0
pydantic-settings>=2.0.0
requests>=2.31.0
gitpython>=3.1.0

# Performance
numpy>=1.24.0
