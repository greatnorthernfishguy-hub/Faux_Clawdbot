# Python Dependencies for TQB Worker (Faux_Clawdbot)
#
# CHANGELOG [2026-02-14 - Integration]
# Swapped custom xet vector store for NeuroGraph cognitive memory layer.
# [2026-03-29 - Hammer/TQB — Block G Assembly]
# Added anthropic SDK. Removed HuggingFace InferenceClient dependency (model swap).

# Gradio for web interface (5.0+ required for type="messages" format)
gradio>=5.0.0
pytz>=2023.3

# HuggingFace for dataset persistence (notebook + checkpoint sync)
huggingface-hub>=0.20.0

# Model providers — Claude (Anthropic) and OpenRouter (OpenAI-compatible)
# CODEMINE_PROVIDER env var selects: "anthropic" or "openrouter"
anthropic>=0.40.0
openai>=1.30.0

# NeuroGraph cognitive memory layer + ng_embed (Snowflake ONNX, dual-pass)
# [2026-03-30] Replaced sentence-transformers with onnxruntime + tokenizers (ng_embed)
onnxruntime>=1.17.0
tokenizers>=0.15.0
msgpack>=1.0.0
scipy>=1.10.0

# Document ingestion (universal_ingestor)
beautifulsoup4>=4.12.0
PyPDF2>=3.0.0

# Additional utilities
pydantic>=2.0.0
pydantic-settings>=2.0.0
requests>=2.31.0
gitpython>=3.1.0

# Env config
python-dotenv>=1.0.0

# Spec execution (structured work block validation)
jsonschema>=4.20.0

# Performance
numpy>=1.24.0
